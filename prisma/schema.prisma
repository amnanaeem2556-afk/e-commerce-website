// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  firstName String
  lastName  String
  phone     String?
  role      String   @default("USER") // USER or ADMIN
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  orders    Order[]
}

model Product {
  id           String      @id @default(uuid())
  name         String
  slug         String      @unique
  subtitle     String
  description  String
  category     String
  subCategory  String
  collection   String
  price        Float
  stockCount   Int         @default(0)
  inStock      Boolean     @default(true)
  isNew        Boolean     @default(false)
  isSale       Boolean     @default(false)
  isFeatured   Boolean     @default(false)
  isBestSeller Boolean     @default(false)
  images       String      // Stored as JSON string
  colors       String      // Stored as JSON string
  sizes        String      // Stored as JSON string
  details      String      // Stored as JSON string (material, origin, care)
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt
  orderItems   OrderItem[]
  reviews      Review[]
}

model Order {
  id                String      @id @default(uuid())
  orderNumber       String      @unique
  userId            String?
  customerName      String
  email             String
  phone             String
  shippingAddress   String
  city              String
  postalCode        String?
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> 479537bc8f1a769ad4494180d4ea4d73351b05a3
  status            String      @default("confirmed") // confirmed, processing, packed, shipped, in_transit, out_for_delivery, delivered
  paymentMethod     String      // easypaisa, jazzcash, bank_transfer, cod, card
  paymentStatus     String      @default("Completed")
  courierName       String      @default("TCS White-Glove VIP Express")
  trackingNumber    String?
<<<<<<< HEAD
=======
=======
  status            String      @default("pending") // pending, confirmed, processing, shipped, out_for_delivery, delivered, cancelled, returned
  paymentMethod     String      // easypaisa, jazzcash, bank_transfer, cod, card
>>>>>>> dc76fe99c39430892f270c31a641850b11e26596
>>>>>>> 479537bc8f1a769ad4494180d4ea4d73351b05a3
  subtotal          Float
  discount          Float       @default(0)
  shippingFee       Float       @default(0)
  total             Float
  estimatedDelivery String
<<<<<<< HEAD
  itemsData         String?
=======
<<<<<<< HEAD
  itemsData         String?
=======
>>>>>>> dc76fe99c39430892f270c31a641850b11e26596
>>>>>>> 479537bc8f1a769ad4494180d4ea4d73351b05a3
  createdAt         DateTime    @default(now())
  updatedAt         DateTime    @updatedAt

  user       User?       @relation(fields: [userId], references: [id])
  items      OrderItem[]
  payment    Payment?
}

model OrderItem {
  id            String  @id @default(uuid())
  orderId       String
  productId     String
  selectedColor String
  selectedSize  String
  quantity      Int
  price         Float

  order   Order   @relation(fields: [orderId], references: [id], onDelete: Cascade)
  product Product @relation(fields: [productId], references: [id])
}

model Payment {
  id            String   @id @default(uuid())
  orderId       String   @unique
  transactionId String?  @unique
  status        String   @default("pending") // pending, completed, failed
  gateway       String
  amount        Float
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  order Order @relation(fields: [orderId], references: [id], onDelete: Cascade)
}

model Review {
  id           String   @id @default(uuid())
  productId    String
  author       String
  city         String
  rating       Int
  title        String
  comment      String
  verified     Boolean  @default(false)
  helpfulCount Int      @default(0)
  createdAt    DateTime @default(now())

  product Product @relation(fields: [productId], references: [id], onDelete: Cascade)
}
